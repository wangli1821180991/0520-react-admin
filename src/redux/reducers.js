/*根据preState和action来生成newState*/

import {combineReducers } from 'redux';
function xxxx(preState={},action) {
    switch (action.type) {
        default:
            return preState;
    }
}
export default combineReducers({
    xxxx
})